# 英文單字練習 — 修改記錄

---

## v13 (2026-04-18)

### 新增：PWA (Progressive Web App)
- `manifest.json`：名稱、圖示 (192/512/maskable)、主題色、standalone 模式、快捷入口
- `sw.js` service worker：
  - App Shell 預先快取（HTML/CSS/JS/圖示）→ 支援離線開啟
  - Supabase 網域一律走網路（認證、session、同步不快取）
  - stale-while-revalidate 策略 → 背景自動更新
  - 版本控制 + 新版本提示 toast
- 支援「安裝到主畫面」(Android/Chrome/Edge) 與 iOS 的「加入主畫面」

### 新增：手機介面全面優化
- **底部導航列**：手機螢幕 (<768px) 隱藏頂部 tab，改為底部固定 4 格導航（紀錄/練習/遊戲/單字庫），含 iOS 安全區
- **大觸控區**：主要按鈕最小 44–46px，符合 Apple HIG
- **滑動翻卡**：Vocab 練習頁支援左右滑切換單字
- **全螢幕練習模式**：⛶ 按鈕隱藏所有導航，只剩練習內容
- **深色模式**：🌙 切換，首次跟隨系統 `prefers-color-scheme`
- **字體大小切換**：S / M / L 三段，Aa 按鈕循環
- **單手優化**：右下角 FAB 工具列（全螢幕 / 字體 / 深色 / 安裝）
- 防 iOS 輸入框自動縮放（字體 ≥17px）、防雙擊縮放、無選取反白

### 新增：踢下線即時化
- **Supabase Realtime 訂閱**：`vocab_users` UPDATE 事件 → 毫秒級踢下線（原 30 秒輪詢仍保留為備援）
- **visibilitychange**：從背景切回前景時立刻驗證 session
- **pagehide / beforeunload**：離開前強制 flush `_dbSaveNow` → 防資料丟失
- **伺服器端強化** (新 `supabase_v13_upgrade.sql`)：
  - `save_progress` / `get_progress` 新增選填 `p_token` 參數
  - client 每次儲存帶上 session_token，token 不符 → 拒絕寫入 (`session_expired`)
  - 防「殭屍分頁」被踢後仍偷偷寫入髒資料

### 修正：被踢後重複觸發
- `_checkSession` 偵測到踢出後，立刻 `_stopSessionChecker()` 並設 `__kicked` 旗標
- 避免同時多個 async 呼叫重複彈窗、重複清狀態

### 網路狀態
- 頂部紅色條：偵測到離線自動顯示 `⚠ 目前無網路連線，資料同步暫停`
- 一回到線上自動觸發 `_dbSaveNow` + `_checkSession`

### 新增檔案
- `manifest.json` / `sw.js` / `pwa-extras.js`
- `icon-192.png` / `icon-512.png` / `icon-maskable.png`
- `supabase_v13_upgrade.sql`（選擇性升級 SQL）

### 需要執行
1. 部署時把新檔案一起上傳（含 icon 與 sw.js）
2. 於 Supabase SQL Editor 執行 `supabase_v13_upgrade.sql`（升級 RPC + 啟用 Realtime publication）
3. 部署路徑需支援 HTTPS（PWA 必要條件）— GitHub Pages 已符合

---

## v12 (2026-04-16)

### 修正：登入 session 衝突偵測
- 登出時正確清除後端 `session_token`（設為 NULL），避免下次登入誤判「其他裝置在線」
- 新增定期 session 驗證（每 30 秒），登入後如被其他裝置頂掉，會即時顯示被踢出彈窗

### 修正：複習錯誤單字視窗
- 視窗寬度從 480px 擴為 520px，並修正 padding/overflow，所有內容顯示正常不擁擠

### 修正：「✓ 刪除」按鈕始終顯示
- 移除「曾答對過才顯示刪除按鈕」的限制，錯誤單字列表和統計頁清單中所有錯誤單字都顯示刪除按鈕

### 修正：複習常錯文法類型 → 獨立 Modal
- 點擊「複習常錯文法類型」現在開啟獨立的全螢幕 Modal（不再跳到練習頁文法分頁）
- Modal 首頁顯示常錯文法主題列表（含錯誤次數與橫條圖）
- 題目介面與答題流程和複習錯誤單字相同：音效、自動切換、輪次結束統計、全對灑花

### 修正：統計數據日期
- 修正 `_updateDailyLog`、`_logGameScore`、`getStatsForRange` 使用 UTC 日期的問題
- 改為使用本機時區日期（`_localDateStr()`），台灣（UTC+8）使用者不再因為時區偏差導致「本日/本週/本月」統計不正確

---

## v11 (2026-04-16)

### 修正：帳號登入衝突處理
- **同一設備同一帳號**也不再允許同時登入（不同分頁開啟也會被偵測）
- 登入時若偵測到其他裝置/分頁已在線，彈出置中確認視窗：
  「已在其他裝置登入，是否重新登入？（舊裝置將自動登出）」
  - 選擇「重新登入」→ 取得新 session token，舊裝置自動登出
  - 選擇「取消」→ 取消登入，關閉彈窗
- 被踢出的裝置顯示置中彈窗「已在其他裝置登入」，點擊確認後返回登入畫面

### 修正：複習錯誤單字
- 複習視窗改為**垂直置中**顯示（原本從頂部開始）
- 在統計頁和複習 Modal 首頁的錯誤單字列表新增「**✓ 刪除**」按鈕
  - 只有在該單字**曾答對過**後才顯示刪除按鈕
  - 點擊刪除後，該單字從錯誤列表永久移除（不影響練習記錄）
- 複習答題加入**音效**：答對清脆上升音、答錯低沉下降音
- 全部答對時顯示**灑花特效**和「全對！太厲害了！」鼓勵文字 + 音效

### 修正：文法錯誤統計
- 修正「複習常錯文法類型」按鈕點擊後無法切換到文法練習頁面的問題
  （原因：程式碼嘗試查詢不存在的 `.tab[onclick*="grammar"]` 選擇器）

### 新增：數據顯示改進
- 統計頁新增「**本日 / 本週 / 本月**」三個分頁按鈕
  - 本日：顯示今天的練習次數與正確率
  - 本週：匯總最近 7 天的 dailyLogs 數據
  - 本月：匯總最近 30 天的 dailyLogs 數據
- 新增**閱讀測驗**統計記錄（練習次數顯示在統計卡片）
- 新增**遊戲最高分**卡片（打地鼠、翻牌、Wordle、填字遊戲）

---

## v10 (2026-04-15)

### 新增：單裝置登入（session_token 機制）
- 登入時產生 UUID session token 寫入 Supabase
- 每次 syncFromDB 前驗證 token，被其他裝置覆蓋時自動登出

### 新增：資料庫擴充儲存
- `vocab_progress` 新增 `daily_logs` 欄位
- 每日自動記錄練習次數、正確率、所有遊戲分數
- 跨裝置同步，可用於後期統計

### 新增：答題音效
- 所有答題類型（字彙、拼字、文法、程度測驗、打地鼠、翻牌、Wordle、填字）
- 答對：清脆雙音；答錯：低沉下降音

### 修改：自動切換計時器
- 所有自動切換從 2000ms / 1200ms → **1000ms**

---

## v9 (2026-04-14)

### 新增：單字庫擴充至 2547 字
- 參照「國中2000字」PDF 補入 690 個缺失單字
- 補充 128 個現有單字的音標
- 更新 `英文單字庫_v9_完整字庫.xlsx`

---

## v1–v8 (歷史)
- 基礎字彙/拼字/文法練習
- 打地鼠、翻牌、Wordle、填字遊戲
- Supabase 後端登入與進度同步
- 閱讀測驗、程度測驗（CEFR）
- 任務系統、連續天數統計
